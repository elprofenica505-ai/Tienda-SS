'use client';

import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import { TenantProvider, useTenant } from '@/components/tenant/TenantProvider';

type Action = 'view' | 'create' | 'edit' | 'delete' | 'export';
type Module = { key: string; label: string };
type PermissionMatrix = Record<string, Record<Action, boolean>>;
const roleLabels: Record<string, string> = { owner: 'Propietario', admin: 'Administrador', jefe: 'Supervisor', vendedor: 'Vendedor', bodega: 'Bodega', chofer: 'Chofer', cajero: 'Cajero' };
const roleOrder = ['admin', 'jefe', 'vendedor', 'bodega', 'chofer', 'cajero'];
const actionLabels: Record<Action, string> = { view: 'Ver', create: 'Crear', edit: 'Editar', delete: 'Eliminar', export: 'Exportar' };

function PermissionsContent() {
  const router = useRouter(); const { authUser, tenant, member, loading: tenantLoading } = useTenant();
  const [modules, setModules] = useState<Module[]>([]); const [roles, setRoles] = useState<Record<string, PermissionMatrix>>({}); const [selectedRole, setSelectedRole] = useState('vendedor'); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [message, setMessage] = useState('');
  async function load() { if (!authUser || !tenant) return; setLoading(true); try { const response = await fetch('/api/permissions', { headers: { Authorization: `Bearer ${await authUser.getIdToken()}`, 'x-tenant-id': tenant.id }, cache: 'no-store' }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'No se pudieron cargar permisos.'); setModules(data.modules || []); setRoles(data.roles || {}); } catch (error) { setMessage(error instanceof Error ? error.message : 'Error cargando permisos.'); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, [authUser, tenant]);
  function toggle(moduleKey: string, action: Action) { setRoles((current) => ({ ...current, [selectedRole]: { ...current[selectedRole], [moduleKey]: { ...current[selectedRole][moduleKey], [action]: !current[selectedRole][moduleKey][action] } } })); }
  function toggleModule(moduleKey: string, enabled: boolean) { setRoles((current) => ({ ...current, [selectedRole]: { ...current[selectedRole], [moduleKey]: { ...current[selectedRole][moduleKey], view: enabled, create: enabled, edit: enabled, delete: enabled, export: enabled } } })); }
  async function save() { if (!authUser || !tenant || selectedRole === 'owner') return; setSaving(true); setMessage(''); try { const response = await fetch('/api/permissions', { method: 'PATCH', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${await authUser.getIdToken()}`, 'x-tenant-id': tenant.id }, body: JSON.stringify({ role: selectedRole, permissions: roles[selectedRole] }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'No se pudieron guardar permisos.'); setMessage('Permisos guardados para este rol.'); setRoles((current) => ({ ...current, [selectedRole]: data.permissions })); } catch (error) { setMessage(error instanceof Error ? error.message : 'No se pudieron guardar permisos.'); } finally { setSaving(false); } }
  if (tenantLoading || loading) return <div className="workspace-loading">Cargando permisos...</div>;
  if (!authUser || !tenant || !member) { router.replace('/'); return null; }
  const matrix = roles[selectedRole] || {};
  return <main className="workspace-page"><aside className="workspace-sidebar"><div className="onboarding-brand"><span className="brand-mark-icon">N</span><b>Nexo<span>Flow</span></b></div><div className="workspace-company"><span>{tenant.name.slice(0, 1).toUpperCase()}</span><div><b>{tenant.name}</b><small>Plan {tenant.plan || 'Starter'}</small></div></div><nav><a onClick={() => router.push('/workspace')}>▦ <span>Resumen</span></a><a onClick={() => router.push('/workspace/members')}>♙ <span>Usuarios y roles</span></a><a className="active">⚙ <span>Permisos</span></a></nav></aside><section className="workspace-main permissions-main"><header className="permissions-header"><div><button className="text-link" onClick={() => router.push('/workspace')}>← Resumen</button><div className="eyebrow catalog-eyebrow">Tu espacio / Administración</div><h1>Permisos por rol</h1><p>Configura accesos granulares para <strong>{tenant.name}</strong>.</p></div><button className="button" onClick={() => void save()} disabled={saving || selectedRole === 'owner'}>{saving ? 'Guardando...' : 'Guardar cambios ↗'}</button></header>{message && <div className="catalog-message">{message}</div>}<div className="permissions-layout"><aside className="role-list"><div className="eyebrow">Roles</div>{roleOrder.map((role) => <button className={selectedRole === role ? 'selected' : ''} key={role} onClick={() => setSelectedRole(role)}><span>{roleLabels[role]}</span><small>{Object.values(roles[role] || {}).filter((item) => item.view).length} módulos</small></button>)}<div className="owner-note"><b>Propietario</b><small>Siempre tiene acceso total y no se puede restringir.</small></div></aside><div className="permissions-panel"><div className="permissions-panel-head"><div><div className="eyebrow">Configurando</div><h2>{roleLabels[selectedRole]}</h2></div><span>Los cambios aplican solo a este tenant</span></div><div className="permission-table"><div className="permission-row permission-head"><span>Módulo</span>{(Object.keys(actionLabels) as Action[]).map((action) => <span key={action}>{actionLabels[action]}</span>)}</div>{modules.map((module) => { const row = matrix[module.key] || { view: false, create: false, edit: false, delete: false, export: false }; const enabled = Object.values(row).some(Boolean); return <div className="permission-row" key={module.key}><div><b>{module.label}</b><small>{enabled ? 'Acceso configurado' : 'Sin acceso'}</small></div>{(Object.keys(actionLabels) as Action[]).map((action) => <button aria-label={`${module.label}: ${actionLabels[action]}`} className={row[action] ? 'permission-check checked' : 'permission-check'} key={action} onClick={() => toggle(module.key, action)}>{row[action] ? '✓' : ''}</button>)}</div>; })}</div><div className="permission-footer"><span>Activa o desactiva cada permiso individualmente.</span><button className="text-link" onClick={() => modules.forEach((module) => toggleModule(module.key, false))}>Quitar todos</button><button className="text-link" onClick={() => modules.forEach((module) => toggleModule(module.key, true))}>Dar acceso total</button></div></div></div></section></main>;
}

export default function PermissionsPage() { return <TenantProvider><PermissionsContent /></TenantProvider>; }
