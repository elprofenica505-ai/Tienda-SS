'use client';

import { FormEvent, useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import { TenantProvider, useTenant } from '@/components/tenant/TenantProvider';

type Expense = { id: string; description: string; amount: number; category?: string; paymentMethod?: string };
type CashMovement = { id: string; description: string; amount: number; direction: string; paymentMethod?: string };

function FinanceContent() {
  const router = useRouter();
  const { authUser, tenant, member, loading: tenantLoading } = useTenant();
  const [expenses, setExpenses] = useState<Expense[]>([]); const [cashMovements, setCashMovements] = useState<CashMovement[]>([]); const [summary, setSummary] = useState({ income: 0, expenses: 0, adjustments: 0, net: 0 }); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [message, setMessage] = useState(''); const [modal, setModal] = useState<'expense' | 'cash' | null>(null); const [form, setForm] = useState({ description: '', amount: '', category: 'General', paymentMethod: 'cash', direction: 'out', notes: '' });
  async function load() { if (!authUser || !tenant) return; setLoading(true); try { const response = await fetch('/api/finance', { headers: { Authorization: `Bearer ${await authUser.getIdToken()}`, 'x-tenant-id': tenant.id }, cache: 'no-store' }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'No se pudo cargar las finanzas.'); setExpenses(data.expenses || []); setCashMovements(data.cashMovements || []); setSummary(data.summary || { income: 0, expenses: 0, adjustments: 0, net: 0 }); } catch (error) { setMessage(error instanceof Error ? error.message : 'Error cargando finanzas.'); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, [authUser, tenant]);
  function open(type: 'expense' | 'cash') { setForm({ description: '', amount: '', category: 'General', paymentMethod: 'cash', direction: type === 'cash' ? 'in' : 'out', notes: '' }); setModal(type); }
  async function save(event: FormEvent) { event.preventDefault(); if (!authUser || !tenant || !modal) return; setSaving(true); setMessage(''); try { const response = await fetch('/api/finance', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${await authUser.getIdToken()}`, 'x-tenant-id': tenant.id }, body: JSON.stringify({ ...form, type: modal, amount: Number(form.amount) }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'No se pudo registrar.'); setMessage(modal === 'expense' ? 'Gasto registrado.' : 'Movimiento de caja registrado.'); setModal(null); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : 'No se pudo registrar.'); } finally { setSaving(false); } }
  if (tenantLoading || loading) return <div className="workspace-loading">Cargando flujo de caja...</div>;
  if (!authUser || !tenant || !member) { router.replace('/'); return null; }
  return <main className="workspace-page"><aside className="workspace-sidebar"><div className="onboarding-brand"><span className="brand-mark-icon">N</span><b>Nexo<span>Flow</span></b></div><div className="workspace-company"><span>{tenant.name.slice(0, 1).toUpperCase()}</span><div><b>{tenant.name}</b><small>Plan {tenant.plan || 'Starter'}</small></div></div><nav><a onClick={() => router.push('/workspace')}>▦ <span>Resumen</span></a><a onClick={() => router.push('/workspace/sales')}>◈ <span>Ventas / POS</span></a><a className="active">$ <span>Finanzas</span></a><a onClick={() => router.push('/workspace/receivables')}>◷ <span>Cuentas por cobrar</span></a></nav></aside><section className="workspace-main finance-main"><header className="finance-header"><div><button className="text-link" onClick={() => router.push('/workspace')}>← Resumen</button><div className="eyebrow catalog-eyebrow">Tu espacio / Finanzas</div><h1>Flujo de caja</h1><p>Controla ingresos, gastos y movimientos de <strong>{tenant.name}</strong>.</p></div><div className="finance-actions"><button className="button button-secondary" onClick={() => open('cash')}>+ Movimiento de caja</button><button className="button" onClick={() => open('expense')}>+ Registrar gasto</button></div></header>{message && <div className="catalog-message">{message}</div>}<div className="finance-metrics"><div><small>Ingresos registrados</small><strong>${summary.income.toFixed(2)}</strong><span>ventas cobradas</span></div><div><small>Gastos</small><strong>${summary.expenses.toFixed(2)}</strong><span>salidas operativas</span></div><div className={summary.net < 0 ? 'metric-alert' : ''}><small>Flujo neto</small><strong>${summary.net.toFixed(2)}</strong><span>ingresos - gastos + ajustes</span></div></div><div className="finance-layout"><div className="finance-panel"><div className="inventory-panel-header"><div><div className="eyebrow">Egresos</div><h2>Gastos recientes</h2></div></div>{expenses.length === 0 ? <div className="inventory-empty">Todavía no hay gastos registrados.</div> : expenses.slice(0, 10).map((item) => <div className="finance-row" key={item.id}><span className="finance-icon expense">↓</span><div><b>{item.description}</b><small>{item.category || 'General'} · {item.paymentMethod || 'Efectivo'}</small></div><strong>-${Number(item.amount).toFixed(2)}</strong></div>)}</div><div className="finance-panel"><div className="inventory-panel-header"><div><div className="eyebrow">Caja</div><h2>Movimientos</h2></div></div>{cashMovements.length === 0 ? <div className="inventory-empty">Todavía no hay ajustes de caja.</div> : cashMovements.slice(0, 10).map((item) => <div className="finance-row" key={item.id}><span className={`finance-icon ${item.direction === 'in' ? 'income' : 'expense'}`}>{item.direction === 'in' ? '↑' : '↓'}</span><div><b>{item.description}</b><small>{item.paymentMethod || 'Efectivo'}</small></div><strong className={item.direction === 'in' ? 'finance-income' : ''}>{item.direction === 'in' ? '+' : '-'}${Number(item.amount).toFixed(2)}</strong></div>)}</div></div>{modal && <div className="modal-backdrop" onClick={() => setModal(null)}><div className="catalog-modal" onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={() => setModal(null)}>×</button><div className="eyebrow">{modal === 'expense' ? 'Nuevo gasto' : 'Movimiento de caja'}</div><h2>{modal === 'expense' ? 'Registra una salida.' : 'Ajusta el flujo.'}</h2><form onSubmit={save}><label>Descripción<input required value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} placeholder={modal === 'expense' ? 'Ej. Renta del local' : 'Ej. Fondo inicial'} /></label><label>Monto<input required type="number" min="0.01" step="0.01" value={form.amount} onChange={(event) => setForm({ ...form, amount: event.target.value })} placeholder="0.00" /></label>{modal === 'expense' ? <label>Categoría<select value={form.category} onChange={(event) => setForm({ ...form, category: event.target.value })}><option>General</option><option>Operación</option><option>Personal</option><option>Servicios</option><option>Compras</option><option>Impuestos</option></select></label> : <label>Dirección<select value={form.direction} onChange={(event) => setForm({ ...form, direction: event.target.value })}><option value="in">Entrada</option><option value="out">Salida</option></select></label>}<label>Método de pago<select value={form.paymentMethod} onChange={(event) => setForm({ ...form, paymentMethod: event.target.value })}><option value="cash">Efectivo</option><option value="card">Tarjeta</option><option value="transfer">Transferencia</option></select></label><label>Notas<input value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} placeholder="Opcional" /></label><button className="button auth-submit" disabled={saving}>{saving ? 'Guardando...' : 'Guardar registro ↗'}</button></form></div></div>}</section></main>;
}

export default function FinancePage() { return <TenantProvider><FinanceContent /></TenantProvider>; }
