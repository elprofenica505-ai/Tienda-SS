'use client';

import { WorkspaceSidebar } from '@/components/workspace/WorkspaceSidebar';

import { useCallback, useEffect, useMemo, useState } from 'react';
import { useRouter } from 'next/navigation';
import { useTenant } from '@/components/tenant/TenantProvider';
import { formatMoney } from '@/lib/currency';
import { getClientCached, invalidateClientCache } from '@/lib/client-query-cache';

type Product = { id: string; name: string; sku?: string; itemType: string; stock: number; price: number; categoryId?: string };
type Customer = { id: string; name: string; email?: string; phone?: string; active: boolean };
type CartLine = Product & { quantity: number };
type Sale = { id: string; saleNumber?: string; invoiceNumber?: string; total: number; paymentMethod: string; customerName?: string; customerId?: string; createdAt?: unknown };
type PosReceipt = { saleId?: string; saleNumber?: string; invoiceNumber?: string; total: number; paymentMethod: string; cashReceived?: number; changeAmount?: number; items: CartLine[]; customerName: string; issuedAt: string };
type SalesCatalogResponse = { products?: Product[] };
type SalesResponse = { sales?: Sale[] };
type CustomersResponse = { contacts?: Customer[] };

function SalesContent() {
  const router = useRouter();
  const { authUser, tenant, member, activeBranchId, organization, loading: tenantLoading } = useTenant();
  const [products, setProducts] = useState<Product[]>([]);
  const [customers, setCustomers] = useState<Customer[]>([]);
  const [sales, setSales] = useState<Sale[]>([]);
  const [cart, setCart] = useState<CartLine[]>([]);
  const [query, setQuery] = useState('');
  const [customerQuery, setCustomerQuery] = useState('');
  const [paymentMethod, setPaymentMethod] = useState('cash'); const [cashReceived, setCashReceived] = useState('');
  const [customerId, setCustomerId] = useState('');
  const [discount, setDiscount] = useState('');
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [message, setMessage] = useState('');
  const [receipt, setReceipt] = useState<PosReceipt | null>(null);

  const load = useCallback(async () => {
    if (!authUser || !tenant) return;
    setLoading(true);
    try {
      const token = await authUser.getIdToken();
      const headers = { Authorization: `Bearer ${token}`, 'x-tenant-id': tenant.id, ...(activeBranchId ? { 'x-branch-id': activeBranchId } : {}) };
      const catalog = await getClientCached<SalesCatalogResponse>(`catalog:${authUser.id}:${tenant.id}:active:first`, async () => { const response = await fetch('/api/catalog', { headers, cache: 'no-store' }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || 'No se pudo cargar el catálogo.'); return payload as SalesCatalogResponse; }, 60_000);
      const salesData = await getClientCached<SalesResponse>(`sales:${authUser.id}:${tenant.id}:${activeBranchId || 'all'}:first`, async () => { const response = await fetch('/api/sales', { headers, cache: 'no-store' }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || 'No se pudo cargar ventas.'); return payload as SalesResponse; }, 10_000);
      const customersData = await getClientCached<CustomersResponse>(`contacts:${authUser.id}:${tenant.id}:customer:active`, async () => { const response = await fetch('/api/contacts?type=customer', { headers, cache: 'no-store' }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || 'No se pudieron cargar clientes.'); return payload as CustomersResponse; }, 60_000);
      setProducts(catalog.products || []); setSales(salesData.sales || []); setCustomers(customersData.contacts || []);
    } catch (error) { setMessage(error instanceof Error ? error.message : 'No se pudo cargar el punto de venta.'); }
    finally { setLoading(false); }
  }, [authUser, tenant, activeBranchId]);
  useEffect(() => { void load(); }, [load]);

  const filtered = products.filter((item) => `${item.name} ${item.sku || ''}`.toLowerCase().includes(query.toLowerCase()));
  const filteredCustomers = customers.filter((item) => `${item.name} ${item.email || ''} ${item.phone || ''}`.toLowerCase().includes(customerQuery.toLowerCase()));
  const selectedCustomer = customers.find((item) => item.id === customerId);
  const subtotal = useMemo(() => cart.reduce((sum, item) => sum + Number(item.price || 0) * item.quantity, 0), [cart]);
  const total = Math.max(0, subtotal - Math.max(0, Number(discount || 0)));
  function add(product: Product) { setCart((current) => { const found = current.find((item) => item.id === product.id); if (found) return current.map((item) => item.id === product.id ? { ...item, quantity: Math.min(item.quantity + 1, product.itemType === 'service' ? 999 : product.stock) } : item); return [...current, { ...product, quantity: 1 }]; }); }
  function changeQuantity(id: string, delta: number) { setCart((current) => current.flatMap((item) => { if (item.id !== id) return [item]; const max = item.itemType === 'service' ? 999 : item.stock; const quantity = Math.min(max, item.quantity + delta); return quantity > 0 ? [{ ...item, quantity }] : []; })); }
  async function checkout() { const activeWarehouseId = organization?.warehouses.find((item) => item.branchId === activeBranchId && item.active)?.id || organization?.warehouses.find((item) => item.active)?.id; if (!authUser || !tenant || !cart.length || !activeBranchId || !activeWarehouseId) { setMessage('Configura un almacén activo para esta sucursal antes de vender.'); return; } setSaving(true); setMessage(''); try { const response = await fetch('/api/sales', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${await authUser.getIdToken()}`, 'x-tenant-id': tenant.id, 'x-branch-id': activeBranchId }, body: JSON.stringify({ items: cart.map((item) => ({ productId: item.id, quantity: item.quantity, unitPrice: item.price })), paymentMethod, cashReceived: paymentMethod === 'cash' ? Number(cashReceived || 0) : undefined, customerId: customerId || null, discount: Number(discount || 0), branchId: activeBranchId, warehouseId: activeWarehouseId }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'No se pudo completar la venta.'); invalidateClientCache(`sales:${authUser.id}:${tenant.id}:`); invalidateClientCache(`catalog:${authUser.id}:${tenant.id}:`); invalidateClientCache(`inventory:${authUser.id}:${tenant.id}:`); setReceipt({ saleId: data.saleId, saleNumber: data.saleNumber, invoiceNumber: data.invoiceNumber, total: Number(data.total || total), paymentMethod, cashReceived: paymentMethod === 'cash' ? Number(cashReceived || 0) : undefined, changeAmount: paymentMethod === 'cash' ? Math.max(0, Number(cashReceived || 0) - total) : 0, items: cart, customerName: selectedCustomer?.name || 'Venta mostrador', issuedAt: new Date().toISOString() }); setMessage(`Venta ${data.saleId} registrada por ${money(Number(data.total))}.`); setCart([]); setCustomerId(''); setCustomerQuery(''); setDiscount(''); setCashReceived(''); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : 'No se pudo completar la venta.'); } finally { setSaving(false); } }

  if (tenantLoading || loading) return <div className="workspace-loading">Cargando punto de venta...</div>;
  if (!authUser || !tenant || !member) { router.replace('/'); return null; }
  const money = (value: number) => formatMoney(value, tenant.currency, tenant.locale);
  return <main className="workspace-page"><WorkspaceSidebar /><section className="workspace-main sales-main"><header className="sales-header"><div><button className="text-link" onClick={() => router.push('/workspace')}>← Resumen</button><div className="eyebrow catalog-eyebrow">Tu espacio / Ventas</div><h1>Punto de venta</h1><p>Registra ventas en <strong>{organization?.branches.find((branch) => branch.id === activeBranchId)?.name || 'la sucursal activa'}</strong> de {tenant.name}.</p></div><span className="catalog-isolation">● Venta transaccional</span></header>{message && <div className="catalog-message">{message}</div>}{receipt && <section className="section-card fiscal-receipt-card receipt-printable" role="status"><div className="eyebrow">Comprobante generado</div><h2>{receipt.invoiceNumber || receipt.saleNumber || receipt.saleId}</h2><p>{receipt.customerName} · {new Intl.DateTimeFormat(tenant.locale || 'es-NI', { dateStyle: 'short', timeStyle: 'medium' }).format(new Date(receipt.issuedAt))}</p><div className="receipt-items">{receipt.items.map((item, index) => <div key={`${item.id}-${index}`}><span>{item.quantity} × {item.name}</span><b>{money(Number(item.price) * item.quantity)}</b></div>)}</div><div className="cash-summary-grid"><div><small>Total</small><b>{money(receipt.total)}</b></div><div><small>Método</small><b>{receipt.paymentMethod}</b></div><div><small>Recibido</small><b>{receipt.cashReceived === undefined ? '—' : money(receipt.cashReceived)}</b></div><div><small>Vuelto</small><b>{receipt.changeAmount === undefined ? '—' : money(receipt.changeAmount)}</b></div></div><div className="ticket-actions no-print"><button className="button" type="button" onClick={() => window.print()}>Imprimir / Guardar PDF</button><button className="button button-secondary" type="button" onClick={() => setReceipt(null)}>Nueva venta</button></div></section>}<div className="sales-layout"><div className="sales-products"><div className="sales-toolbar"><div><div className="eyebrow">Catálogo disponible</div><h2>Selecciona productos</h2></div><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar nombre o SKU" /></div><div className="sales-product-grid">{filtered.length === 0 ? <div className="catalog-empty"><div className="empty-spark">◇</div><h2>No hay productos disponibles</h2><p>Agrega productos en el catálogo antes de registrar una venta.</p><button className="button" onClick={() => router.push('/workspace/catalog')}>Ir al catálogo ↗</button></div> : filtered.map((product) => <button className="sales-product" key={product.id} onClick={() => add(product)} disabled={product.itemType !== 'service' && product.stock <= 0}><span className="sales-product-icon">◇</span><b>{product.name}</b><small>{product.sku || 'Sin SKU'} · {product.itemType === 'service' ? 'Servicio' : `${product.stock} disponibles`}</small><strong>{money(Number(product.price || 0))}</strong></button>)}</div></div><aside className="sale-ticket"><div className="ticket-head"><div><div className="eyebrow">Nueva venta</div><h2>Ticket</h2></div><span>{cart.length} ítems</span></div><div className="ticket-lines">{cart.length === 0 ? <div className="ticket-empty"><span>+</span><p>Selecciona productos<br />para iniciar la venta</p></div> : cart.map((item) => <div className="ticket-line" key={item.id}><div><b>{item.name}</b><small>{money(Number(item.price))} c/u</small></div><div className="quantity-control"><button onClick={() => changeQuantity(item.id, -1)}>−</button><span>{item.quantity}</span><button onClick={() => changeQuantity(item.id, 1)}>+</button></div><strong>{money(Number(item.price) * item.quantity)}</strong></div>)}</div><div className="ticket-form"><label>Cliente guardado {paymentMethod === 'credit' && <small className="required-note">(obligatorio para crédito)</small>}<select required={paymentMethod === 'credit'} value={customerId} onChange={(event) => { setCustomerId(event.target.value); setCustomerQuery(''); }}><option value="">{paymentMethod === 'credit' ? 'Selecciona un cliente' : 'Venta mostrador'}</option>{filteredCustomers.map((customer) => <option key={customer.id} value={customer.id}>{customer.name}{customer.phone ? ` · ${customer.phone}` : ''}</option>)}</select></label><input className="customer-search" value={customerQuery} onChange={(event) => setCustomerQuery(event.target.value)} placeholder="Filtrar clientes guardados" />{selectedCustomer && <div className="selected-customer">Cliente seleccionado: <strong>{selectedCustomer.name}</strong></div>}{paymentMethod === 'cash' && <label>Efectivo recibido<input type="number" min={total} step="0.01" value={cashReceived} onChange={(event) => setCashReceived(event.target.value)} placeholder={money(total)} /><small className={Number(cashReceived || 0) < total ? 'required-note' : undefined}>{Number(cashReceived || 0) < total ? `Faltan ${money(total - Number(cashReceived || 0))}` : `Vuelto: ${money(Number(cashReceived || 0) - total)}`}</small></label>}<div className="payment-label">Método de pago</div><div className="payment-options">{[['cash','Efectivo'],['card','Tarjeta'],['transfer','Transferencia'],['credit','Crédito']].map(([value, label]) => <button key={value} className={paymentMethod === value ? 'selected' : ''} onClick={() => setPaymentMethod(value)}>{label}</button>)}</div><label>Descuento<input type="number" min="0" step="0.01" value={discount} onChange={(event) => setDiscount(event.target.value)} placeholder="0.00" /></label></div><div className="ticket-total"><div><span>Subtotal</span><strong>{money(subtotal)}</strong></div><div><span>Descuento</span><strong>-{money(Math.max(0, Number(discount || 0)))}</strong></div><div className="total-line"><span>Total</span><strong>{money(total)}</strong></div></div><button className="button button-large checkout-button" onClick={checkout} disabled={saving || !cart.length || !activeBranchId || (paymentMethod === 'credit' && !customerId) || (paymentMethod === 'cash' && Number(cashReceived || 0) < total)}>{saving ? 'Procesando...' : 'Cobrar venta ↗'}</button></aside></div><div className="recent-sales"><div className="inventory-panel-header"><div><div className="eyebrow">Historial</div><h2>Ventas recientes</h2></div></div>{sales.length === 0 ? <div className="inventory-empty">Las ventas registradas aparecerán aquí.</div> : sales.slice(0, 6).map((sale) => <div className="recent-sale" key={sale.id}><span className="sale-status">✓</span><div><b>{sale.saleNumber || sale.id}</b><small>{sale.invoiceNumber ? `Factura ${sale.invoiceNumber} · ` : ''}{sale.customerName || 'Venta mostrador'} · {sale.paymentMethod}</small></div><strong>{money(Number(sale.total || 0))}</strong></div>)}</div></section></main>;
}

export default function SalesPage() {
  return <SalesContent />; }
