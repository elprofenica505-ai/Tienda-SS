'use client';

import { WorkspaceSidebar } from '@/components/workspace/WorkspaceSidebar';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { useRouter } from 'next/navigation';
import { TenantProvider, useTenant } from '@/components/tenant/TenantProvider';

type Product = { id: string; name: string; sku?: string; itemType: string; stock: number; price: number };
type CartLine = Product & { quantity: number };
type PreSale = { id: string; ticketCode: string; total: number; status: string; createdAt?: unknown };

function PresalesContent() {
  const router = useRouter(); const { authUser, tenant, member, loading: tenantLoading } = useTenant();
  const [products, setProducts] = useState<Product[]>([]); const [presales, setPresales] = useState<PreSale[]>([]); const [cart, setCart] = useState<CartLine[]>([]); const [query, setQuery] = useState(''); const [evidence, setEvidence] = useState<string[]>([]); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [message, setMessage] = useState('');
  const load = useCallback(async () => { if (!authUser || !tenant) return; setLoading(true); try { const headers = { Authorization: `Bearer ${await authUser.getIdToken()}`, 'x-tenant-id': tenant.id }; const [catalogResponse, presalesResponse] = await Promise.all([fetch('/api/catalog', { headers, cache: 'no-store' }), fetch('/api/presales', { headers, cache: 'no-store' })]); const catalog = await catalogResponse.json(); const data = await presalesResponse.json(); if (!catalogResponse.ok) throw new Error(catalog.error || 'No se pudo cargar el catálogo.'); if (!presalesResponse.ok) throw new Error(data.error || 'No se pudieron cargar tus preventas.'); setProducts(catalog.products || []); setPresales(data.presales || []); } catch (error) { setMessage(error instanceof Error ? error.message : 'No se pudo cargar preventas.'); } finally { setLoading(false); } }, [authUser, tenant]);
  useEffect(() => { void load(); }, [load]);
  const filtered = products.filter((item) => `${item.name} ${item.sku || ''}`.toLowerCase().includes(query.toLowerCase())); const total = useMemo(() => cart.reduce((sum, item) => sum + Number(item.price || 0) * item.quantity, 0), [cart]);
  function add(product: Product) { setCart((current) => { const found = current.find((item) => item.id === product.id); if (found) return current.map((item) => item.id === product.id ? { ...item, quantity: item.quantity + 1 } : item); return [...current, { ...product, quantity: 1 }]; }); }
  function change(id: string, delta: number) { setCart((current) => current.flatMap((item) => { if (item.id !== id) return [item]; const quantity = Math.max(0, item.quantity + delta); return quantity ? [{ ...item, quantity }] : []; })); }
  async function sendToCashier() { if (!authUser || !tenant || !cart.length) return; setSaving(true); setMessage(''); try { const response = await fetch('/api/presales', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${await authUser.getIdToken()}`, 'x-tenant-id': tenant.id }, body: JSON.stringify({ action: 'send', items: cart.map((item) => ({ productId: item.id, quantity: item.quantity })), evidenceRefs: evidence }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'No se pudo enviar la preventa.'); setMessage(`Preventa ${data.ticketCode} enviada a caja.`); setCart([]); setEvidence([]); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : 'No se pudo enviar la preventa.'); } finally { setSaving(false); } }
  if (tenantLoading || loading) return <div className="workspace-loading">Cargando preventas...</div>; if (!authUser || !tenant || !member) { router.replace('/'); return null; }
  return <main className="workspace-page"><WorkspaceSidebar /><section className="workspace-main sales-main"><header className="sales-header"><div><button className="text-link" onClick={() => router.push('/workspace')}>← Resumen</button><div className="eyebrow catalog-eyebrow">Tu espacio / Ventas</div><h1>Preventa en piso</h1><p>Arma el ticket, adjunta evidencia y envíalo a caja para cobrarlo.</p></div><span className="catalog-isolation">● Stock baja al cobrar</span></header>{message && <div className="catalog-message" role="status">{message}</div>}<div className="sales-layout"><div className="sales-products"><div className="sales-toolbar"><div><div className="eyebrow">Catálogo disponible</div><h2>Agrega productos</h2></div><input aria-label="Buscar producto por nombre o SKU" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar nombre o SKU" /></div><div className="sales-product-grid">{filtered.length === 0 ? <div className="catalog-empty"><h2>No hay productos disponibles</h2><p>Solicita al dueño que agregue productos al catálogo.</p></div> : filtered.map((product) => <button className="sales-product" key={product.id} onClick={() => add(product)} disabled={product.itemType !== 'service' && product.stock <= 0}><b>{product.name}</b><small>{product.sku || 'Sin SKU'} · {product.itemType === 'service' ? 'Servicio' : `${product.stock} disponibles`}</small><strong>${Number(product.price || 0).toFixed(2)}</strong></button>)}</div></div><aside className="sale-ticket"><div className="ticket-head"><div><div className="eyebrow">Nueva preventa</div><h2>Ticket</h2></div><span>{cart.length} ítems</span></div><div className="ticket-lines">{cart.length === 0 ? <div className="ticket-empty"><span>+</span><p>Selecciona productos<br />para iniciar</p></div> : cart.map((item) => <div className="ticket-line" key={item.id}><div><b>{item.name}</b><small>${Number(item.price).toFixed(2)} c/u</small></div><div className="quantity-control"><button type="button" onClick={() => change(item.id, -1)}>−</button><span>{item.quantity}</span><button type="button" onClick={() => change(item.id, 1)}>+</button></div><strong>${(Number(item.price) * item.quantity).toFixed(2)}</strong></div>)}</div><label>Evidencia del ticket<input type="file" accept="image/*" capture="environment" onChange={(event) => { const file = event.target.files?.[0]; if (file) setEvidence((current) => [...current, `evidence://${file.name}`].slice(0, 10)); }} /><small>Opcional: cámara o galería. Se guarda una referencia liviana, no la imagen en Firestore.</small></label>{evidence.length > 0 && <div className="selected-customer">Adjuntos: {evidence.join(', ')}</div>}<div className="ticket-total"><div className="total-line"><span>Total</span><strong>${total.toFixed(2)}</strong></div></div><button className="button button-large checkout-button" onClick={sendToCashier} disabled={saving || !cart.length}>{saving ? 'Enviando...' : 'Enviar a caja ↗'}</button></aside></div><div className="recent-sales"><div className="inventory-panel-header"><div><div className="eyebrow">Mis preventas</div><h2>Actividad del día</h2></div></div>{presales.length === 0 ? <div className="inventory-empty">Tus preventas enviadas aparecerán aquí.</div> : presales.map((item) => <div className="recent-sale" key={item.id}><span className="sale-status">{item.status === 'paid' ? '✓' : '•'}</span><div><b>{item.ticketCode}</b><small>{item.status === 'sent_to_cashier' ? 'En caja' : item.status === 'paid' ? 'Pagada' : item.status}</small></div><strong>${Number(item.total || 0).toFixed(2)}</strong></div>)}</div></section></main>;
}
export default function PresalesPage() { return <TenantProvider><PresalesContent /></TenantProvider>; }
